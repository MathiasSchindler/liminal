program ConsultHint;

types
  schema TPerson
    Name: String;
    Age: Integer;
  end;

begin
  WriteLn(consult Oracle from '{"Name":123,"Age":"x"}' into TPerson with attempts: 2 on failure retry with hint 'Name must be string; Age integer'; end);
end.
